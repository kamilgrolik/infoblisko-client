import styled from 'styled-components';
import { ToastHeader } from 'reactstrap';

export const Wrapper = styled.div`
  margin: 40px 0;
`;

export const StyledToastHeader = styled(ToastHeader)`
  color: #fff;
  font-size: 1.25rem;
`;
